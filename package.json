{
  "name": "evidence-ai-explorer",
  "version": "1.0.0",
  "description": "AI-powered data exploration - GitHub Pages compatible",
  "scripts": {
    "dev": "python -m http.server 8000",
    "build": "echo 'No build step needed - static files ready for GitHub Pages'",
    "deploy": "echo 'Push to gh-pages branch or enable GitHub Pages on main branch'"
  },
  "dependencies": {},
  "devDependencies": {}
}